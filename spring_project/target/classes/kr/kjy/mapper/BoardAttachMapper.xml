<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="kr.kjy.mapper.BoardAttachMapper">
	<insert id = "insert">
		insert into tbl_attach (uuid, uploadpath, filename, filetype, bno)
		values (#{uuid}, #{uploadpath}, #{filename}, #{filetype}, #{bno})
	</insert>
	<delete id="delete">
		delete from tbl_attch where uuid = #{uuid}
	</delete>
	<select id="findByBno" resultType="kr.kjy.model.BoardAttachVO">
		select * from tbl_attach where bno = #{bno}
	</select>
	<delete id="deleteAll">
		delete from tbl_attach where bno = #{bno}
	</delete>
	<select id="getOldFiles"  resultType="kr.kjy.model.BoardAttachVO">
		select * from tbl_attach where uploadpath = DATE_FORMAT(date_add(now(), interval -1 day), '%Y\%m\%d')
	</select>
</mapper>